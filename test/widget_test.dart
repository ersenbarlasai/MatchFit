import 'package:flutter_test/flutter_test.dart';

import 'package:matchfit/main.dart';

void main() {
  testWidgets('MatchFitApp can be constructed', (WidgetTester tester) async {
    expect(const MatchFitApp(), isA<MatchFitApp>());
  });
}
