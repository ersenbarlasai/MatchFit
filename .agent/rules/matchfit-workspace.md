# MatchFit Workspace Rule

Ana agent mimarisi:

`docs/agents/CANONICAL_AGENT_ARCHITECTURE.md`

Kod yazarken önce owner agent'ı belirle, sonra canonical dosyadaki authority sınırlarını uygula.

Kritik kurallar: Trust Score `@Referee`, bildirim `@Notification`, ekonomi ledger `@EconomyEngine`, ödül önerisi `@RewardPersonalization`, katalog/stok `@PartnerCatalog`, context `@ContextAgent`, analytics `@AnalyticsAgent`.

